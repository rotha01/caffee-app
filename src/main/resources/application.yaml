spring:
  threads:
    virtual:
      enabled: true
  application:
    name: caffee-app

server:
  port: 8081

management:
  info:
    env:
      enabled : true
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env,beans